Idea: propose this as "mrt downloaddb"
Download Production DB from yourapp.meteor.com into your local meteor instance
usage:

> asteroidmine.sh YOURAPP.meteor.com [local meteor directory, defaults to .]
